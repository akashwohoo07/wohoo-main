# BullMQ declares an OPTIONAL peer on redis>=5 (node-redis), but we use ioredis
# for BullMQ and redis@4 (node-redis) only for the cache/session layer. The two
# never interact, so we relax strict peer resolution for this benign optional peer.
legacy-peer-deps=true
